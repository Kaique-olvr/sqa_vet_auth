"""
Camada de Rotas / Endpoints HTTP (Controllers).

Objetivo:
- Declarar as rotas da API (ex: POST /auth/register, POST /auth/login).
- Receber as requisições HTTP, repassar para a lógica de negócio e devolver os códigos de status adequados (200, 201, 401, 422).

"""